[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "root_dependency_test_jase231"
version = "0.0.1"
readme = "README.md"
license-files = ["LICENSE"]
dependencies = ["ROOT"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "root_dependency_test_jase231"
version = "0.0.1"
readme = "README.md"
license-files = ["LICENSE"]
# pinned to ROOT version 6.40.0
dependencies = ["ROOT==0.1a12"]
